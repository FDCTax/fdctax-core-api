# FDC Core - Kubernetes Secrets Configuration
# NOTE: This is a TEMPLATE. Actual secrets should be managed via:
# - External Secrets Operator + AWS Secrets Manager/Vault
# - Sealed Secrets
# - Doppler
#
# NEVER commit actual secrets to version control!

apiVersion: v1
kind: Secret
metadata:
  name: fdc-core-secrets
  labels:
    app: fdc-core
type: Opaque
stringData:
  # Database - Use External Secrets Operator in production
  DATABASE_URL: "REPLACE_WITH_EXTERNAL_SECRET"
  
  # JWT Authentication
  JWT_SECRET_KEY: "REPLACE_WITH_EXTERNAL_SECRET"
  
  # Integrations
  CALENDLY_PAT: "REPLACE_WITH_EXTERNAL_SECRET"
  CALENDLY_WEBHOOK_SECRET: "REPLACE_WITH_EXTERNAL_SECRET"
  
  # Error tracking
  SENTRY_DSN: "REPLACE_WITH_EXTERNAL_SECRET"

---
# External Secrets Operator Configuration (Recommended)
# Uncomment and configure when using AWS Secrets Manager
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: fdc-core-secrets
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: ClusterSecretStore
#   target:
#     name: fdc-core-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: DATABASE_URL
#       remoteRef:
#         key: fdc-core/production
#         property: DATABASE_URL
#     - secretKey: JWT_SECRET_KEY
#       remoteRef:
#         key: fdc-core/production
#         property: JWT_SECRET_KEY
#     - secretKey: SENTRY_DSN
#       remoteRef:
#         key: fdc-core/production
#         property: SENTRY_DSN
