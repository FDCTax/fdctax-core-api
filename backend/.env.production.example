# FDC Core - Production Environment Configuration
# Copy this file to .env and update values for production deployment
# NEVER commit actual secrets to version control

# ==================== ENVIRONMENT ====================
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# ==================== DATABASE ====================
# PostgreSQL (Primary) - Use a secure connection string
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DATABASE?ssl=require

# MongoDB (Legacy - being phased out)
MONGO_URL=mongodb://localhost:27017
DB_NAME=fdc_tax_crm

# ==================== AUTHENTICATION ====================
# IMPORTANT: Generate a secure random key for production
# python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=CHANGE_THIS_TO_A_SECURE_64_CHARACTER_RANDOM_STRING
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
JWT_ALGORITHM=HS256

# ==================== CORS ====================
# Production origins only - no localhost
CORS_ORIGINS=https://fdctax.com,https://www.fdctax.com,https://myfdc.com,https://www.myfdc.com,https://api.fdccore.com

# ==================== STORAGE ====================
# Update to S3 when ready
STORAGE_REF_BASE=s3://fdc-core-uploads
UPLOAD_MAX_SIZE_MB=50

# ==================== INTEGRATIONS ====================
CALENDLY_PAT=YOUR_CALENDLY_PAT
CALENDLY_WEBHOOK_SECRET=YOUR_WEBHOOK_SECRET

# ==================== OBSERVABILITY ====================
SENTRY_DSN=YOUR_SENTRY_DSN

# ==================== API ====================
API_TITLE=FDC Tax Core API
API_VERSION=1.0.0
API_RATE_LIMIT=100

# ==================== DEPLOYMENT CHECKLIST ====================
# Before deploying to production, ensure:
# [ ] JWT_SECRET_KEY is a unique, secure random string
# [ ] DATABASE_URL points to production database
# [ ] CORS_ORIGINS does not include localhost
# [ ] DEBUG is set to false
# [ ] SENTRY_DSN is configured for error tracking
# [ ] All secrets are stored in a secure vault, not in this file
