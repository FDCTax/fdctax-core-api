# FDC Core - Staging Environment Configuration Template

# ==================== ENVIRONMENT ====================
ENVIRONMENT=staging
DEBUG=false
LOG_LEVEL=DEBUG

# ==================== DATABASE ====================
DATABASE_URL=${EXTERNAL_SECRET:DATABASE_URL_STAGING}

# ==================== AUTHENTICATION ====================
JWT_SECRET_KEY=${EXTERNAL_SECRET:JWT_SECRET_KEY_STAGING}
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
JWT_ALGORITHM=HS256

# ==================== CORS ====================
# Staging + local dev origins
CORS_ORIGINS=https://staging.fdctax.com,https://staging.myfdc.com,https://staging-api.fdccore.com,http://localhost:3000,http://localhost:5173

# ==================== STORAGE ====================
STORAGE_REF_BASE=s3://fdc-core-uploads-staging
UPLOAD_MAX_SIZE_MB=50

# ==================== INTEGRATIONS ====================
CALENDLY_PAT=${EXTERNAL_SECRET:CALENDLY_PAT}
CALENDLY_WEBHOOK_SECRET=${EXTERNAL_SECRET:CALENDLY_WEBHOOK_SECRET}

# ==================== OBSERVABILITY ====================
SENTRY_DSN=${EXTERNAL_SECRET:SENTRY_DSN_STAGING}

# ==================== API ====================
API_TITLE=FDC Tax Core API (Staging)
API_VERSION=1.0.0
API_RATE_LIMIT=100
